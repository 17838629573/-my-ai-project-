# 失败与边界记录（ERRORS）

> 长期记忆层 + 人类在环闸门。AI 只记录与提示，删除 / 固化权归用户。

## 写入触发
- `skills/halt/SKILL.md` 触发时追加一条；步骤中碰到能力边界（工具缺失 / 服务不通 / 搜索无果）也追加
- 同一根因 / 同一错误特征反复（如反复 ImportError）→ 在同一 key 下累加计数（AI 不确定时宁按同类累加，交用户判断）

## 记录格式
```
[日期] 步骤=XX | 类型=能力缺失/超时/循环/搜索无果/同类错误
原因：<一句话>
分支：<halt 四选项之一 / 索要 / 降级>
结果：<用户怎么处理 / 待处理>
```

## 交用户阈值（均为建议值、留余量）
- 同类失败累积约 3 次（用户可随时提前查看，不必死等）
- 单次即导致流程完不成

## 用户动作（三选一）
- **固化**：有价值 → 改成对应 skill 的新规则 / 新铁律
- **留档**：有价值但暂不固化，继续观察
- **删除**：确认无意义 → 删除并留一行痕迹：`YYYY-MM-DD 删 N 条，原因：<…>`

## 禁止
AI 静默删除条目 · AI 自行把条目固化成规则（只可"建议固化"）· 条目消失不留痕

[2026-09-08] 步骤=4集成 | 类型=能力缺失
原因：需要 docker 跑集成测试，但环境无 docker（真实探测 MISSING）
分支：索要 / 降级
结果：待用户选——提供docker / 换纯python降级方案
